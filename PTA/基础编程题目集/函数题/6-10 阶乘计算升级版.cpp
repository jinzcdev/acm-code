// https://pintia.cn/problem-sets/14/problems/742
#include <stdio.h>
#include <math.h>
void Print_Factorial ( const int N ) {
    int ans[1000];
    ans[0] = 1;
    for (int i = 1; i <= N; i++) {

    }
    

}

int main()
{
    // int N;
	
    // scanf("%d", &N);
    // Print_Factorial(N);
    printf("%d", (int)sqrt(145));
    
    return 0;
}