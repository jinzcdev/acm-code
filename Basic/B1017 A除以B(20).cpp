#include <cstdio>
#include <cstring>
const int n = 1010;
char a[n];
int b;

int getRemainder(int ){

}

int main(){
	scanf("%s %d",a,&b);
	int len = strlen(a),mid = len / 2,rmd;
	getRemainder(0,mid - 1,mid,len - 1);
	return 0;
}